# kubereats-backend


在 **kubereats-backend** 裡執行：

`uv init`

安裝後端套件：

這會產生類似：

`<span><span>pyproject.toml uv.lock </span></span>`

這就像前端的：

`<span><span>package.json package-lock.json</span></span>`
