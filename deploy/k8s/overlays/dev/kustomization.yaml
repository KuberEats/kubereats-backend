apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubereats-dev
resources:
  - ../../base
images:
  - name: ghcr.io/kubereats/kubereats-verification
    newTag: dev
patches:
  - target:
      kind: ConfigMap
      name: verification-config
    patch: |-
      - op: replace
        path: /data/APP_ENV
        value: dev
  - target:
      kind: Deployment
      name: verification
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/imagePullPolicy
        value: IfNotPresent
