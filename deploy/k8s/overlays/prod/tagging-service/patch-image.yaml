apiVersion: apps/v1
kind: Deployment
metadata:
  name: tagging-service
spec:
  template:
    spec:
      containers:
        - name: api
          image: ghcr.io/kubereats/tagging-service:TODO
          imagePullPolicy: Always
