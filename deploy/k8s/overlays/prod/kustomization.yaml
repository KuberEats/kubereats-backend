apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kubereats-prod
resources:
  - namespace.yaml
  - merchant-service
  - committee-service
  - notification-service
  - finance-service
  - order-scheduler-service
  - recommendation-service
  - tagging-service
  - verification-service
# Production is sample-only until backend-prod.yaml.sample is copied to a real Application.
# ingress.example.yaml is optional and intentionally not included.
